# PraveshPath — Handoff Document
**Date:** 2026-05-28
**Author:** Aarya (Rajtaya@kuk.ac.in)

---

## Project Overview
**PraveshPath** (formerly CED Platform) — A web app for Haryana university admissions eligibility matching. Students enter their academic profile and the platform auto-matches them with eligible programmes across Haryana universities based on qualification, marks, stream, and subjects.

- **Backend:** Django 6 + Django REST Framework, JWT auth (simplejwt + token blacklist), Google OAuth
- **Frontend:** React 19 + Vite 8, React Router, Axios, PWA enabled
- **Database:** SQLite (local), PostgreSQL (Railway production)
- **Local repos:** `/Users/aarya/CED_Platform/backend` and `/Users/aarya/CED_Platform/frontend`

---

## Live Deployment

| Service | URL |
|---------|-----|
| Frontend | https://parveshpath.up.railway.app |
| Backend API | https://backend-production-9c253.up.railway.app |
| Railway Dashboard | https://railway.com/project/07c53558-e014-470d-9075-d6cb1535e170 |
| GitHub (primary) | https://github.com/Rajtaya/PraveshPath |
| GitHub (old) | https://github.com/Rajtaya/CED_Platform |

### Production Data (Railway PostgreSQL)

| Entity | Count |
|--------|-------|
| Universities | 7 (KUK, MDU, GJUS&T, GU, CUH, CCSHAU, CRSU) |
| Courses | 415 |
| University-Courses (Offerings) | 450 |
| Eligibility Criteria | 450 |
| Admission Cycles | 450 |
| Required Documents | 12 |
| University-Course Documents | 684 |

---

## Changes Made Today (2026-05-28, Session continued)

### 1. Replaced UG/PG Filter with Qualification-Based Matching
- **Removed** the 4-step wizard with UG/PG card selection (Step 1: Select Level)
- **Added** 3-step wizard: Academic Info → Preferences → Confirm
- **New "Highest Qualification" dropdown** with 4 options:
  - Higher Secondary / 10+2
  - Polytechnic Diploma
  - Graduate
  - Post-Graduate
- Conditional form rendering based on qualification:
  - Higher Secondary / Graduate / Post-Graduate → shows 12th fields
  - Graduate / Post-Graduate → also shows graduation fields
  - Polytechnic Diploma → shows diploma stream only
- **Matching engine auto-determines eligibility** via `QUALIFICATION_ELIGIBLE_LEVELS`:
  - `higher_secondary` → UG, diploma, certificate
  - `polytechnic_diploma` → UG, diploma, certificate
  - `graduate` → UG, PG, diploma, certificate
  - `post_graduate` → UG, PG, PhD, diploma, certificate
- Removed `preferred_level` field from model; added `highest_qualification`, `graduation_percentage`, `graduation_stream`, `graduation_subject`
- Removed 10th percentage from the form (user request)
- Migration: `students/0004_remove_preferred_level_add_qualification`

### 2. Subject Checkboxes (replacing comma-separated input)
- Replaced the "12th Subjects (comma-separated)" text input with **stream-specific checkboxes**
- Subjects shown based on selected 12th stream:
  - **Science:** Physics, Chemistry, Mathematics, Biology, Computer Science, Biotechnology, English, Physical Education, Hindi, Economics
  - **Commerce:** Accountancy, Business Studies, Economics, Mathematics, English, Computer Science, Physical Education, Hindi
  - **Arts:** History, Geography, Political Science, Sociology, Psychology, Economics, English, Hindi, Sanskrit, Home Science, Physical Education, Mathematics
- Changing stream clears subject selection
- Legacy "Math"/"Maths" values normalized to "Mathematics" on profile load

### 3. Full 7-University Dataset Deployed to Production
- Previously production only had KUK + MDU (2 universities, 197 offerings)
- **Now deployed all 7 universities** with 418 offerings to Railway PostgreSQL
- Data loaded via `loaddata` in Railway start command (internal network, fast)
- Fixture file: `backend/fixtures/all_universities_data.json` (30k+ lines)
- Fixed `entrance_exam` field: increased `max_length` from 100 to 300 (PostgreSQL enforces varchar limits, SQLite doesn't)
- Migration: `core/0003_increase_entrance_exam_length`

### 4. Frontend Deployed to Railway
- Frontend service updated with all wizard changes and subject checkboxes
- Profile wizard route is `/profile` (not `/wizard`)

### 5. Earlier Changes (from morning session)
- Rebranded to PraveshPath (logo, navbar, favicon)
- PWA support (service worker, manifest, icons)
- Google OAuth configured
- Railway deployment set up (backend + frontend + PostgreSQL)

### 6. UI Overhaul (evening session)
- Complete redesign: CSS custom properties design system, SVG icons replacing emojis
- Frosted glass navbar with mobile hamburger menu + slide-out drawer
- Home page: gradient hero, animated stat counter (IntersectionObserver), features grid, how-it-works flow, CTA card, footer
- Login/Signup: split-panel layout with gradient left panel, password strength meter
- Profile wizard: SVG step indicators with glow ring, pill-shaped checkbox chips
- Browse: IISc-style redesign (see §10 below for details)
- Results: pill-shaped badges, spinner loading, consistent `var(--radius-xl)` styling
- GJUS&T admission portal link updated across all 69 admission cycles
- PWA enabled in dev mode (`devOptions: { enabled: true }`), Apple meta tags added

### 7. Matching Engine Optimization (evening session)
- Eliminated double query in `find_eligible_courses` — was collecting IDs then re-querying, now returns list directly
- Added DB-level `course__level__in` filter (was filtering in Python after fetch)
- Changed `eligible.count()` → `len(eligible)` in views (avoids extra DB round-trip)
- Added `db_index=True` on `Course.level` and `Course.stream`
- Result: 13-40ms per match query (was significantly slower)

### 8. Database Optimization (evening session)
- Added indexes on `is_active` (University, Course, UniversityCourse), `academic_year`, `status` (AdmissionCycle)
- Fixed N+1 in `universities_with_programmes`: 1 query per university → 2 queries total via `Prefetch` with `to_attr`
- `UniversityCourseViewSet` list: added filtered prefetch for `current_cycles`, serializer uses prefetched data
- `university_programmes`: filtered `Prefetch` for 2026-2027 cycles only (was fetching all years)
- `platform_stats`: 4 separate COUNT queries → 3 via `aggregate`
- All major endpoints now run with constant query count regardless of data size

### 9. Security Hardening (evening session)
**Backend:**
- `SECRET_KEY` crashes at startup in production if env var missing (was insecure fallback)
- `DEBUG` defaults to `False` (was `True`)
- Default DRF permission changed to `IsAuthenticated` (was `AllowAny`)
- Rate limiting: 30/min anonymous, 120/min authenticated, 5/min on login/register/google-auth
- JWT token blacklisting enabled (`rest_framework_simplejwt.token_blacklist` + `BLACKLIST_AFTER_ROTATION`)
- Production security headers: HSTS (1 year), SSL redirect, secure cookies, referrer policy, `SECURE_PROXY_SSL_HEADER`
- Google OAuth now verifies `email_verified` claim (rejects unverified emails)
- `quick_match` rewritten: uses in-memory `StudentProfile` object (zero DB writes, no orphan rows)
- Percentage fields (10th, 12th, graduation) validated to 0-100 range via `MinValueValidator`/`MaxValueValidator`
- Removed hardcoded Google OAuth client ID fallback from settings

**Frontend:**
- Content Security Policy meta tag: restricts script/style/connect sources
- `upgrade-insecure-requests` meta tag for HTTPS enforcement
- API cache (`api-cache`) cleared on logout (PWA service worker was persisting authenticated responses)
- `.env` added to `.gitignore` (was missing, could be accidentally committed)

### 10. Desktop Modal Fix — DetailModal outside animated container (2026-05-28)
- **Bug:** Course detail popup was invisible on desktop — overlay rendered but off-screen
- **Root cause:** `DetailModal` was rendered inside `.results-page` div which has `animation: fadeInUp 0.4s ease both`. CSS transforms from the animation create a new containing block, breaking `position: fixed` on the overlay. The overlay's `top: 0` was relative to the animated container (at y=-681px) instead of the viewport.
- **Fix:** Wrapped `Results` component return in React fragment `<>...</>`, moved `DetailModal` outside `.results-page` as a sibling:
  ```jsx
  return (
    <>
      <div className="results-page">...</div>
      {selectedProg && <DetailModal ... />}
    </>
  )
  ```
- **Verified:** Overlay parent is now `main-content` (no transform), overlay at `top: 0, left: 0` covering full viewport, modal centered
- **File:** `frontend/src/pages/Results.jsx` — lines 72, 308-316

### 11. Mobile Course Popup — Full-screen slide-in page (2026-05-28)
- Replaced the centered modal on mobile (< 600px) with a full-screen white page that slides in from the right
- Sticky frosted-glass header bar with SVG chevron + "Back to Results" text
- iOS-style `animation: screenSlide 0.25s ease` (translateX 100% → 0)
- `-webkit-overflow-scrolling: touch` for smooth iOS scrolling
- `backdrop-filter: blur(12px)` on header for frosted glass effect

### 12. Back Button Improvements (2026-05-28)
- "Back to Universities" and "Back to Results" buttons restyled across both Browse and Results pages
- Pill-shaped buttons: `background: #eef2ff`, `border: 1.5px solid #c7d2fe`, `color: #1a56db`
- SVG chevron icon (not arrow), `border-radius: 12px`, `box-shadow: 0 1px 3px rgba(26,86,219,0.1)`
- Hover fills solid blue with white text
- Hardcoded colors used instead of CSS variables for reliability

### 13. CCSHAU PG Courses Updated (2026-05-28)
- **Imported 52 courses** from `Data /Updated HAU_PG_Course_Eligibility.xlsx` (18 net new, 34 updated)
- Source: CCSHAU Prospectus 2026-27, Chapter-IV (eligibility) and Chapter-V (seat matrix)
- **New courses include:** M.F.Sc. Aquaculture, Aquatic Animal Health, Fish Engineering, Fisheries Extension/Economics/Resource Mgmt, M.Sc. Biotechnology variants (Agricultural, Molecular, Bio-Nano, Industrial), M.Sc. Bioinformatics, MBA (General/Agri-Business) at Hisar & IBMA Gurugram, Master in Rural Management
- **Stream mapping:** `fisheries` → `agriculture` (no fisheries choice in model; agriculture covers it)
- **Eligibility column fix:** New file uses `min_qualifying_percentage` (old used `min_bachelor_percentage`)
- **Domicile fix:** Handles `=FALSE()` Excel formula residue in `domicile_required` column
- **Default dates updated:** 2026-07-01 to 2026-08-31 for cycles with no explicit dates (was 2025 dates)
- Import script: `backend/core/management/commands/import_ccshau_pg.py` — updated EXCEL_PATH, removed STREAM_MAP (now reads stream directly from Courses sheet), fixed column names
- **DB totals after import:** 7 universities, 415 courses, 450 university-courses

### 15. Frontend URL Changed & Google OAuth Fix (2026-06-02)
- **Frontend URL changed** from `frontend-production-864e.up.railway.app` to `https://parveshpath.up.railway.app`
- **Google sign-in was broken** — "Google sign-in failed" error on signup/login pages
- **Root cause:** Backend `FRONTEND_URL` env var on Railway still pointed to the old frontend URL, so CORS blocked the Google auth callback from the new domain
- **Fix (Railway):** Updated `FRONTEND_URL` to `https://parveshpath.up.railway.app` via `railway variables set`
- **Fix (local dev):** Added `python-dotenv` to backend — `backend/.env` now loaded by `settings.py` so `GOOGLE_OAUTH_CLIENT_ID` is available locally without exporting env vars manually
- **Files changed:**
  - `backend/config/settings.py` — added `from dotenv import load_dotenv` + `load_dotenv(BASE_DIR / '.env')`
  - `backend/.env` — created with `GOOGLE_OAUTH_CLIENT_ID`
  - `backend/requirements.txt` — added `python-dotenv==1.2.2`

### 14. KUK PG Courses Updated (2026-05-28)
- **Replaced old 78 KUK PG courses** with updated 92-course dataset from HBI-2026
- Source: `Data /KUK_PG_Course_Eligibility.xlsx` (Handbook of Information 2026)
- **14 new courses** added: French/German cert/diploma (C703-C708), Panjabi cert (C712), Communication Skills cert (C713), Urdu cert/diploma (C714-C715), Yoga diploma (C737), Reasoning diploma (C738), Bhagvadgita cert (C739), Moral & Spiritual Counseling cert (C740), Sanskrit diploma (C742), B.Ed Special Education (C727), LL.B Professional 3yr (C747)
- **Streams properly mapped** from simplified arts/commerce/science to granular Django choices:
  - MBA/MTTM/MHM&CT → management; LL.M/LL.B → law; M.Ed/B.Ed → education
  - M.P.Ed/B.P.Ed/M.A. Yoga → sports; M.Tech → engineering; MCA/M.Sc CS → computer
  - M.Pharmacy → pharmacy; MFA/M.A. Music/M.A. Fine Arts → design
- Fixed truncated course names (C710, C746, C753, C754, C756, C758, C762, C775, C777, C790)
- Fixed NaN duration_years with correct values
- All 92 courses have seats data, entrance exam info, eligibility criteria, and 2026-2027 admission cycles
- New fixture PKs: courses 419-510, UCs/ECs/ACs 880-971
- **Browse views updated**: Postgraduate toggle now includes diploma/certificate/phd levels (was exact `level=pg` only)
- **Deployment fix**: Added `loaddata` to `railway.toml` start command — runs inside container on internal network (fast and reliable, no more slow external loaddata)

### 11. Browse Page Redesign — IISc-Inspired (2026-05-23)
- **Complete rewrite of `Browse.jsx`** — modelled after IISc Bangalore's programme browser
- **Left filter sidebar** with collapsible sections:
  - University Type (checkbox: State / Central)
  - District (checkbox: all unique districts from data)
  - Level (radio: Undergraduate / Postgraduate)
  - Each section has info icon (ⓘ) + chevron toggle, matching IISc style
  - "Clear all" button at top
  - Sticky positioning on desktop; slide-in drawer overlay on mobile
- **University cards** (3-column grid):
  - Hash-generated gradient image header per university (unique color per name)
  - University short name label on gradient with dark overlay
  - Circular logo with initial letter + full university name + programme count
  - Outlined "View Programmes" button at card bottom
  - Hover: lift + shadow effect
- **Programme view** (after clicking a university):
  - Back button, university header with logo circle, name, type badge, district, "Visit Website" link
  - **Stream filter pills** — All / Arts / Commerce / Computer / Design / Education / Law / Management / Medical / Science / Other
  - Programme cards with **stream-specific gradient headers** (amber for arts, blue for commerce, indigo for computer, etc.)
  - Expandable "More info" button reveals: status, seats, fee, entrance exam, deadline
  - "Any" stream displays as "General" with distinct gradient
- **New CSS badge styles**: `.badge-state`, `.badge-central`, `.badge-unknown`
- **Responsive**: 3-col grid → 2-col (tablet) → 1-col (mobile), filter sidebar → drawer
- All class names prefixed with `b-` to avoid global collisions

---

## Deployment Architecture

```
GitHub (Rajtaya/PraveshPath)
  │
  ├── Backend Service (railway up from backend/)
  │   ├── Nixpacks build (auto-detected Python)
  │   ├── railway.toml: migrate → loaddata → collectstatic → gunicorn
  │   ├── PostgreSQL (Railway managed, internal network)
  │   └── WhiteNoise (static files)
  │
  └── Frontend Service (railway up from frontend/)
      ├── Nixpacks build (auto-detected Node/Vite)
      ├── PWA (service worker + manifest)
      └── Env: VITE_API_URL, VITE_GOOGLE_CLIENT_ID
```

**NOTE:** Deployment currently uses `railway up` (CLI upload), NOT GitHub auto-deploy. To enable auto-deploy:
1. Railway dashboard → each service → Settings → Source → Connect `Rajtaya/PraveshPath`
2. Backend root directory: `backend`
3. Frontend root directory: `frontend`

### Key Deployment Files
| File | Purpose |
|------|---------|
| `backend/railway.toml` | Backend build + start command (migrate → loaddata → collectstatic → gunicorn) |
| `Dockerfile` | Backend Docker build (at repo root, NOT used by Railway — Railway uses Nixpacks) |
| `backend/fixtures/all_universities_data.json` | Full 7-university fixture (2396 objects, auto-loaded on every deploy) |
| `.gitignore` | Excludes Data/, files*/, *.zip, *.pdf, staticfiles/ |

### Railway CLI Quick Reference
```bash
# Deploy backend
cd ~/CED_Platform/backend && railway service backend && railway up

# Deploy frontend
cd ~/CED_Platform/frontend && railway service frontend && railway up
cd ~/CED_Platform/backend && railway service backend  # switch back

# Check logs
railway logs

# Run Django command against production DB
DATABASE_URL="postgresql://postgres:REDACTED_ROTATE_THIS_PASSWORD@ballast.proxy.rlwy.net:29600/railway" \
  ~/CED_Platform/backend/venv/bin/python manage.py <command>
```

---

## Git Remotes

| Remote | URL | Purpose |
|--------|-----|---------|
| `origin` | https://github.com/Rajtaya/CED_Platform.git | Original repo |
| `praveshpath` | https://github.com/Rajtaya/PraveshPath.git | Railway deployment repo |

Always push to both: `git push origin main && git push praveshpath main`

---

## How to Run Locally

```bash
# Backend (DEBUG=True required for local dev since default is now False)
cd /Users/aarya/CED_Platform/backend
source venv/bin/activate
DEBUG=True python manage.py runserver

# Frontend
cd /Users/aarya/CED_Platform/frontend
npm run dev
```

- Backend: http://localhost:8000
- Frontend: http://localhost:5173 (or 5174/5175)

---

## Key Backend Files

| File | Purpose |
|------|---------|
| `students/models.py` | StudentProfile with `highest_qualification`, graduation fields |
| `students/serializers.py` | Profile serializer with all new fields |
| `matcher/engine.py` | Eligibility matching: `QUALIFICATION_ELIGIBLE_LEVELS`, subject matching, criteria checks |
| `matcher/views.py` | `/api/match/` (authenticated) and `/api/quick-match/` (anonymous, rate-limited, in-memory) |
| `core/models.py` | University, Course, UniversityCourse, EligibilityCriteria, AdmissionCycle (indexed) |
| `core/views.py` | Browse endpoints with optimized prefetch queries; PG toggle includes diploma/cert/phd |
| `config/settings.py` | CORS, JWT + blacklist, throttling, security headers |

## Key Frontend Files

| File | Purpose |
|------|---------|
| `src/pages/ProfileWizard.jsx` | 3-step wizard with qualification dropdown, subject checkboxes |
| `src/pages/Results.jsx` | Match results display |
| `src/App.jsx` | Routes — wizard at `/profile`, results at `/results` |
| `src/api/client.js` | Axios API client |

---

## Test Users

### Production (Railway)
| Email | Name | Notes |
|-------|------|-------|
| teststudent@example.com | Test Student | Password: TestPass@1234 |

### Local
| Email | Name | Notes |
|-------|------|-------|
| pooja@example.com | Pooja Sharma | Science, 86%, password: test1234 |
| panchnsdshodhsansthan@gmail.com | panchnad shodhsansthan | Google sign-up |

---

## Known Issues / TODO

### Immediate
- [ ] Set up GitHub auto-deploy on Railway (currently manual `railway up`)
- [ ] CCSHAU, CUH admission portal links — not set (GJUS&T done)
- [ ] CUH PG data — only UG imported (7 courses)
- [ ] Password reset flow — not implemented
- [x] Deploy Browse redesign to Railway (deployed 2026-05-23 via `railway up`)
- [x] Deploy backend changes to Railway (security hardening, DB optimization)
- [x] KUK PG courses updated to 92 (from 78) — deployed 2026-05-28
- [x] Add `loaddata` to Railway start command (permanent fix for data deployment)
- [x] Fix desktop modal invisible — moved DetailModal outside animated container (2026-05-28)
- [x] Mobile popup reworked as full-screen slide-in page with sticky back header (2026-05-28)
- [x] Back buttons made more prominent — pill shape, blue bg/border, SVG chevron (2026-05-28)
- [x] CCSHAU PG courses updated — 52 courses from Updated HAU_PG_Course_Eligibility.xlsx (2026-05-28)

### Security (remaining lower priority)
- [ ] Move JWT tokens from localStorage to httpOnly cookies (requires backend cookie-based auth refactor)
- [ ] Add email verification on registration (needs email service: SendGrid/SES)
- [ ] Check if `db.sqlite3` is in git history — purge with BFG if so
- [ ] Migrate inline `<style>` to CSS modules to remove `unsafe-inline` from CSP

### Post-MVP
- [ ] Email/SMS notifications for deadline reminders
- [ ] Mobile responsive refinements
- [ ] Admin dashboard for university data managers
- [ ] Entrance exam score filtering
- [ ] Category-based seat reservation display

---

*Last updated: 2026-06-02 by Claude*
